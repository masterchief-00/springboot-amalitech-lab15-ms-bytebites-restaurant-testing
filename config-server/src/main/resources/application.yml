server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/masterchief-00/springboot-amalitech-lab15-ms-config-repo.git
          clone-on-start: true
          force-pull: true
